include "default.conf"

# host.mail="http://ubuntu64:8080"

mail {
  client.user = user
  client.password = password

  server = smtp.yandex.ru
  user = "masterj.mail@yandex.ru"
  password = masterj13
  fromName = MasterJ.Mail
  smtpPort = 465
  useSSL = true

  scanTODO=10s
  scanFail=60s
  poolSize=30
}
